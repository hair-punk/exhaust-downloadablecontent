import React from 'react';
import { shallow } from 'enzyme';
import App from '../client/src/index';
import GameAddOns from '../client/src/components/GameAddOns';
import DLC from '../client/src/components/DLC';
import PopOver from '../client/src/components/PopOver';


describe('App component', () => {
  // renders
  // currently does not receive props, will when incorporated into proxy
  it('works', () => {});
});

describe('GameAddOns component', () => {
  // renders
  // receives props
  it('works', () => { });
});

describe('DLC component', () => {
  // renders
  // receives props
  it('works', () => { });
});

describe('PopOver component', () => {
  // renders
  // receives props
  it('works', () => { });
});

